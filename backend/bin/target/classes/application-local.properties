# Local developer overrides — DO NOT COMMIT
# Fill in your values after rotating the Supabase DB password.
# This file is gitignored and never committed.

# Supabase DB password (rotate at: Dashboard → Project Settings → Database → Reset password)
spring.datasource.password=MyTadika2026

# JWT signing secret (any random 256-bit base64 string is fine for local dev)
# Generate one with: openssl rand -base64 32
app.jwt.secret=pIhvY6QTUXw3g5GX7jdEEhiDmTDMcAiyZ0BUddcrhdg=

# Frontend base URL — used to build the reset-password link in emails
app.base-url=http://localhost:3000

# Gmail SMTP for forgot-password emails
spring.mail.username=liewwaikit5pgta5@gmail.com
spring.mail.password=sekg hccg efrt szuu

# Temporary debug logging
spring.jpa.show-sql=true
logging.level.com.mytadika=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web.cors=DEBUG
